# Smart-Digit-Classifier

This project implements a handwritten digit recognition system using OpenCV and K-Nearest Neighbors (KNN) algorithm. 

It processes input images, extracts features, and predicts digits with trained model data.

Future improvements include real-time detection and deep learning integration.
